# Wheel fix packet
Decision: carry-forward. The source fix is built and browser-proved; installed native deployment remains open.

Both Claude/Codex host allowances are visible regardless of conversation provider. Missing values remain unavailable. The ring still represents conversation context. Click opens a persistent scrolling popup; Escape/outside press dismisses it.

Typecheck and web export pass. Browser: 5/5. Full unit suite passed 4880/4880 under bundled Node24 before the interaction repair; final broad rerun had one unrelated setup timeout, and its isolated rerun plus affected tests passed 17/17. Node26 initially failed with storage-environment errors; Homebrew Node22 could not load its library; neither is reported as product success.

Semgrep has non-zero relevant coverage with no findings; Gitleaks objective history has no findings. Trivy covers four unchanged lockfiles and reports 480 dependency vulnerabilities (11 critical, 270 high, 165 medium, 34 low); no dependency-clean claim. See proof.json for exact commands/coverage/exclusions.

Air app 0.7.2 is vendor-signed and seals app-dist; direct asset replacement would invalidate signing. Original remains intact. Native installation/activation, touch, cold dependency reproduction and daemon-backed E2E remain unproved. No Go files changed; Go gates inapplicable. Serena symbol navigation available; Context7/codebase memory unavailable, trust downgraded.

Next authorised work: harden rotation nonce/limit/readiness handling on a dependent clean branch. Automatic replacement policy remains undecided; no account/model switch or conversation replacement is enabled by this work.

Native staging supplement: a separate locally ad-hoc-signed app is prepared on Air at /Users/robwork/Applications/Paseo Local Preview.app. All 39 asset hashes match the tested web export (native-preview.json). Both the preview and untouched original pass codesign strict/deep verification. Preview has not been launched; actual native wheel interaction remains unproved. Activation would switch the visible app and requires the operator's launch instruction under no-popup discipline.
